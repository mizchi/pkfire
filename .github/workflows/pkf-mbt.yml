name: pkf

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "moon.mod.json"
      - "examples/basic/**"
      - ".github/workflows/pkf-mbt.yml"
  pull_request:
    paths:
      - "src/**"
      - "moon.mod.json"
      - "examples/basic/**"
      - ".github/workflows/pkf-mbt.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-smoke:
    name: Build + smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install MoonBit CLI
        run: |
          curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash
          echo "$HOME/.moon/bin" >> "$GITHUB_PATH"

      - name: Moon version
        run: moon version --all

      - name: Moon update
        run: moon update

      - name: Moon check
        # NOT --deny-warn — pkf intentionally carries a few pre-existing
        # @debug warnings; cleaning those is tracked separately. CI catches
        # actual errors (compile + smoke) for now.
        run: moon check --target native

      - name: Build pkf (release)
        run: moon build --target native --release

      - name: Smoke - pkf list (examples/basic)
        working-directory: examples/basic
        run: ../../_build/native/release/build/src/cmd/pkf/pkf.exe list

      - name: Smoke - pkf graph (examples/basic)
        working-directory: examples/basic
        run: ../../_build/native/release/build/src/cmd/pkf/pkf.exe graph

      - name: Smoke - pkf run hello (cache miss + cache hit)
        working-directory: examples/basic
        env:
          PKFIRE_MBT_CACHE_DIR: ${{ runner.temp }}/pkfire-mbt-cache
        run: |
          rm -rf "$PKFIRE_MBT_CACHE_DIR"
          ../../_build/native/release/build/src/cmd/pkf/pkf.exe run hello
          ../../_build/native/release/build/src/cmd/pkf/pkf.exe run hello
